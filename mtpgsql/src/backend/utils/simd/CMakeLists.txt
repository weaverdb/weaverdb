# SIMD utilities - mostly header-only for intrinsics
# Include this dir for headers.
# Consumers can add compile options for SIMD (e.g. -mavx2).

# Placeholder to satisfy build if needed.
add_library(simd INTERFACE)